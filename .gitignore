# Python
__pycache__/
*.py[cod]
*$py.class
venv/
ENV/

# Pliki wykonywalne i binarne (nie wrzucamy ich na GitHub)
dist/
build/
*.exe
*.zip
ffmpeg.exe
ffprobe.exe

# Logi i konfiguracje
*.log
settings.json

# Dokumentacja generowana
DOKUMENTACJA.html
DOKUMENTACJA.pdf

# macOS
.DS_Store

# PyInstaller spec (generated automatically)
*.spec
# Zero-dependency: bundlowane binarki (ffmpeg/ffprobe/yt-dlp/deno) — pobierane przez build_local.sh
bin/

# Model Whisper base.pt (145 MB) przekracza twardy limit GitHuba (100 MB/plik) —
# zostaje tylko w lokalnym builderze (assets/models/whisper/), nie w historii Git.
# tiny.pt (75 MB) jest bundlowany, zostaje śledzony.
assets/models/whisper/base.pt

# Node.js (animejs, eksploracyjne, niepowiązane z aplikacją Python/PyQt6)
node_modules/
package.json
package-lock.json

# Konfiguracje edytora / narzędzi lokalnych
.claude/
.vscode/
